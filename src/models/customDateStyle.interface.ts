export interface CustomDateStyle {
  date: Date;
  style: DateStyles;
}

interface DateStyles {
  backgroundColor: string;
  borderWidth: number;
  borderColor: string;
}

interface DateTextStyle {
  color: string;
}
